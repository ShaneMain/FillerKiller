# Monorepo root. Each part (api/, web/) has its own .gitignore too.

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Env files (never commit real ones; *.example is allowed)
.env
*.env
!*.env.example
